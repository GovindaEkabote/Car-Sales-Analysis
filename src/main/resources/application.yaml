server:
  port: 8081
  servlet:
    context-path: /car-sale-analytics

spring:
  application:
    name: Car-Sale-Analytics

  datasource:
    url: jdbc:mysql://localhost:3306/car_sale_analytics
    username: root
    password: Govinda@1997
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true